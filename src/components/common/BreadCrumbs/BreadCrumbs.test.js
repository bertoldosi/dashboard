import React from "react";
import { screen, render } from "@testing-library/react";
import BreadCrumbs from ".";

describe("first", () => {
  test("should first", () => {
    render(<BreadCrumbs />);
  });
});
